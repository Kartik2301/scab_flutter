import 'package:flutter/material.dart';

class InRoom extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('You son of a bitch, you\'re In ')),
    );
  }
}
